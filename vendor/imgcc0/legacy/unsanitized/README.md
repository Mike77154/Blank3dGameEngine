# Quarantined legacy infrastructure

These files belonged to the pre-0.2 facade path and are intentionally excluded from the strict build.

- `src/zlib_shim.c`
- `shims/zlib.h`
- `compat/stdint.h`

They are kept only as migration/reference material. Do not add these directories to the strict Makefile include or source lists.
