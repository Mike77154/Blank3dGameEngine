# PCX 1.3.0

- Added ELF SONAME/version-script based shared-library governance.
- Added export allowlist verification (`make abi-check`).
- Added first-class CMake package export and `find_package(pcx CONFIG)` support.
- Added sanitizer convenience targets and a consumer-package smoke test.
