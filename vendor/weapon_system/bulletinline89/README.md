# bulletinline89

C89/no-heap convergence helper. A projectile may originate off the weapon's center line; this library recomputes a normalized direction from that real origin to the shared aim target. If no target is available it normalizes the caller's fallback direction.

This makes arbitrary spawn geometries compatible with a single authoritative aim point without coupling the geometry to cameras, actors or weapons.
