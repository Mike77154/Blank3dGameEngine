# gplayerprojectileaim89

Local-player physical projectile aim using a provider-selected camera target.

- C89
- caller-owned/static state
- no malloc/calloc/realloc/free
- no float/double
- no OS dependency

See `include/gplayerprojectileaim89.h` for the public API and `tests/` for a minimal regression.
