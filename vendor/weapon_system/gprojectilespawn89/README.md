# gprojectilespawn89

Provider bus for projectile world/collision/render/audio/publish stages.

- C89
- caller-owned/static state
- no malloc/calloc/realloc/free
- no float/double
- no OS dependency

See `include/gprojectilespawn89.h` for the public API and `tests/` for a minimal regression.
