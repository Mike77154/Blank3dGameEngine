# wsoundprop89

Speed-of-sound delay, distance attenuation, muzzle directivity, occlusion and distance-dependent low-pass. Delay memory is supplied by the host, so the module never allocates. Distances use centimeters.
