# Declarative placeholder consumed by the engine's customcalled/draw-script provider.
# It intentionally is not executed by CPython inside Blank3D.
draw_capsule(self, radius=0.55, height=1.45)
draw_cube(self, socket="head", x=0.55, y=0.55, z=0.55)
