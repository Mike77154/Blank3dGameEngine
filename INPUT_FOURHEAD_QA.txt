Blank3D four-head input integration QA
Date: 2026-08-11

PASS  input_keys89 standalone strict C89 tests
      make test-input-keys-vendor
      -std=c89 -pedantic -Wall -Wextra -Werror

PASS  canonical vocabulary agreement
      make test-input-fourhead-vocab
      Covers input_keys89 <-> polls89 legacy bridge <-> input_hook89
      and input_scanner89 keyboard canonical helpers.

PASS  end-to-end neutral chain with fake raw provider
      make test-input-fourhead-chain
      input_keys89 -> polls89-style raw API -> input_hook89 -> input_scanner89
      Covers Up/W pressed, hold, and released transitions.

PASS  existing Blank3D DDSL input regression
      make test-input-stack
      Output: Blank3D universal input stack test: OK

PASS  complete active source syntax on Linux host
      make syntax-check

PASS  forced Win32 four-head syntax path using declaration stubs
      make syntax-check-input-win32
      This checks the new Win32 polls -> input_hook path without requiring a
      MinGW linker on this host. It is not a substitute for the final MinGW
      executable link on Windows/MSYS2.

PASS  no allocator calls in active integration
      make audit
      Also manually scanned active four-head sources for
      malloc/calloc/realloc/free: none found.

PLAYER DDSL STATUS
      scripts/player.ddsl2 remains arrow-only:
        If key_hold Up then move_forward
        If key_hold Down then move_back
        If key_hold Left then strafe_left
        If key_hold Right then strafe_right
      WASD was not reintroduced.

ARCHITECTURE STATUS
      - all four libraries preserved
      - input_keys89 is canonical key identity
      - polls89 owns native acquisition / legacy physical bindings
      - input_hook89 normalizes provider state into common HID capture
      - input_scanner89 owns temporal semantics
      - Blank3D input core owns no Win32 backend object
      - platform selection is isolated in blank3d_input_platform.*

WIN32 MINGW REGRESSION FIX (2026-08-11)
      - removed nonexistent VK_STOP/VK_UNDO/VK_CUT/VK_COPY/VK_PASTE/VK_FIND
        references from polls89 Win32 and Win64 GetAsyncKeyState backends
      - the corresponding canonical USB HID identities remain valid in
        input_keys89 but report unsupported in this specific polling backend
      - corrected tests/platform_stubs/windows.h so the shim cannot invent
        those VK names and mask a real MinGW failure again
      - forced Win32 syntax path: PASS, no warnings/errors
      - forced Win64 polls89 backend syntax: PASS with -Werror
      - blank3d_list_cycle optimized warning regression: PASS with -Werror
